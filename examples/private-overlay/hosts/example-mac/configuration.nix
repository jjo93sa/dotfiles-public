{...}: {
  # Add private Darwin settings and modules here. The reusable system,
  # development, macOS-defaults, and Homebrew capabilities are selected in the
  # overlay flake so this file contains only host-specific differences.
}
